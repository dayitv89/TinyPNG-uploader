var config = require('config');

var tinify = require('tinify');

// TODO: read recursive folder name as terminal argument
// TODO: Find all images with supported file image extension (use config.IMAGE_EXTENSIONS)
// TODO: Validate image size(use config.IMAGE_SIZE), log error IF exceed.
// TODO: upload files using queue (use this variable to config.QUEUE_SIZE)
// TODO: download all files to respective file path.
// TODO: log error IF any occurs.
